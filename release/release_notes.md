## v2.1.1.12

## v2.1.1.12

### 🐛 修复 / 优化（代码审查后）
- 修复资源包「调整设置」中「当前子包」高亮永远读不到的问题（改为按 pack_id 匹配，符合 BDS 规范）
- 修复列表型配置项编辑后保存静默破坏 JSON 结构（改为 json.loads 解析回写）
- 新增：v3 manifest 自定义设置（开关 / 滑块 / 下拉）现在可编辑并持久化到包内 server_overrides.json（服务器端记录覆盖值，客户端仍可自调）
- 修复自更新解压会扁平化子目录的问题，现保留子目录结构并自动去除整体打包的多余顶层目录（Zip-Slip 防护保留）
- 移除失效的单文件 .py 降级下载死分支（缺 download_url 时直接中文报错）
- 自动备份改为后台线程执行，保留「已打包 N 个文件」进度提示，大世界不再卡死界面；完成后自动恢复服务器
- 服务器输出改为按系统代码页优先解码（系统编码 → utf-8 → gbk/cp936 → latin-1），中文日志不再乱码
- 新增网络错误中文 + 英文 Toast 提示（超时 / DNS 失败 / 连接被拒 / 网络不可达 / SSL 失败 / HTTP 错误），已接入版本检查、工具下载、启动检查、BDS 下载四处

### 🎨 流畅度与美观打磨
- 控制台输出改为「缓冲 + 80ms 定时批量刷新」，整批仅一次重绘；文本 HTML 转义防注入；限制最大 5000 行；仅当用户已在底部时才自动滚动（向上翻历史不再被拽回）
- 仪表盘改为复用 SystemMonitor 的采样结果，消除每 2 秒主线程阻塞 100ms 的卡顿
- 玩家列表仅在集合变化时重建（不再每 2 秒闪烁 / 丢失选中）
- 备份目录扫描改为最多每 15 秒一次（减少磁盘 I/O）
- 修复玩家右键菜单重复弹出两个菜单的 Bug
- 修复标签页标题「升级&&安装」误显示为带下划线加速键的问题 → 改为「升级 / 安装」
- 进度条去掉分段块状观感，改为平滑填充
- 窗口设置最小尺寸 960×620，缩太小布局不再崩
- 状态栏版本号对比度提升（#555 → #888）
- 标签页切换动画改为仅淡入新页面，过渡更干净、重绘更轻
