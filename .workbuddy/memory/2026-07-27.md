# 2026-07-27 工作日志

## lip 一键部署 (BDS + LeviLamina)
- 新增 `backend/lip_utils.py`：lip 安装检测、LipCmdWorker (subprocess.Popen 实时流式输出)、InstallLipWorker
- `pages/upgrade.py`：lip 部署卡片 + 交互终端（ANSI 转义 → HTML 渲染，\r 处理，appndHtml）
- 加速源按钮：`lip config set github_proxy/go_module_proxy`
- 安装完成后自动设置 `server_type=ll`、`ll_server_dir`
- InstallWorker 修复：新安装（do_backup=False）不跳过 resource_packs/behavior_packs

## 双服务器类型 (BDS / BDS+LL)
- 新增 `server_type` 配置键（"bds"/"ll"）
- 控制台页面新增 ComboBox 切换服务器类型 + blockSignals 防初始化覆盖
- `ServerContext` 重构：`server_dir` 等改为动态 @property，根据 server_type 返回 BDS/LL 目录
- `refresh_context_from_config()`：运行时切换后刷新上下文
- 启动逻辑自动检测 LL exe，LL 找不到时 fallback 到候选目录

## 配置落盘修复
- `BOOL_FIELDS` 误将 `server_root_dir`/`ll_server_dir`/`server_type` 分类为 bool → 移到字符串字段
- `_browse_dir/deploy_dir/ll_dir/bds_exe` 四处的 `setText()` 补了 `_silent_save()`

## 代码质量
- fuck-u-code 审查：78/100，微臭青年
- `server_lifecycle.py`：`start_server()` 拆出 `_ensure_default_properties` + `_try_find_ll_exe`

## 工具
- 归档：`py_compile main.py pages/*.py backend/*.py shared/*.py components/*.py && archive.py --force`
- 审查：`fuck-u-code.cmd analyze "项目路径" --top 20 --locale zh --format txt`
